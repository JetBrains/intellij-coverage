sourceSets {
    main.java.srcDirs = []
    test.java.srcDirs = [file('src')]
}

dependencies {
    compile project(':util')
    compile project(':instrumentation')
    compile project(':testDiscovery')

    testCompile 'junit:junit:4.12'
    testCompile rootProject
}