import org.gradle.internal.jvm.Jvm

//noinspection GroovyAssignabilityCheck
version "$version"

apply from: 'project-libraries.gradle'

allprojects {
  repositories {
    mavenCentral()
  }

  apply plugin: 'java'
  sourceCompatibility = 1.6
  targetCompatibility = 1.6

  configurations {
    testArtifacts.extendsFrom testRuntime
  }

  dependencies {
    runtime files(Jvm.current().toolsJar)
  }

  sourceSets {
    main.java.srcDirs 'src'
  }
}

//noinspection GrUnresolvedAccess
task wrapper(type: Wrapper) {
  gradleVersion = '4.5'
}

task copyAsm(type: Copy) {
  from library_project_asm_all
  into file("dist")
}

task copyTrove(type: Copy) {
  from library_project_trove4j
  into file("dist")
}

//subprojects.each { subproject -> evaluationDependsOn( subproject.path ) }
//jar.dependsOn subprojects.tasks['classes']
//jar {
//  baseName = 'MyApp'
//  manifest {
//    attributes 'Main-Class': 'org.abc.App'
//  }
//  ["util"].collect({project(":$it")}).each { subproject ->
//    from subproject.sourceSets.main.output.classesDir
//    from subproject.sourceSets.main.output.resourcesDir
//  }
//}

task coveraAgentJar(type: Jar) {
//  baseName = "coverage-agent"
  archiveName = "coverage-agent.jar"
  destinationDir = file("dist")
  manifest.attributes(
      "Premain-Class": "com.intellij.rt.coverage.main.CoveragePremain",
      "Boot-Class-Path": "coverage-agent.jar asm-all.jar trove4j.jar"
  )
  from(sourceSets.main.output)
  from(["util", "instrumentation"].collect({ project(":$it").sourceSets.main.output }))
}

build.dependsOn(copyAsm, copyTrove, coveraAgentJar)