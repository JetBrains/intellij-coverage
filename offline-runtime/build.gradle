import com.intellij.rt.coverage.build.ArtifactBuilder

dependencies {
  compile(project(":common"))
}

ArtifactBuilder.setUpArtifact(project, offline_jar_name, [":common", ":offline-runtime:java7-utils"])
